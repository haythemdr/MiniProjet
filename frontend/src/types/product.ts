export interface Product {
  name: string;
  price: string;
  image: string;
  url: string;
  store: string;
  score: number;
}

export interface ProductDetails {
  name: string;
  price: string;
  image: string;
  availability: string;
  description: string;
}